---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hark-88594ebc                  # Ingress name
  labels:
    app: hark-88594ebc
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "hark-88594ebc.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hark-88594ebc               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
